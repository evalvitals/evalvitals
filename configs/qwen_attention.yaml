# Minimal config. `model` is a spec key (evalvitals.list_specs()); a legacy alias
# like `qwen` also resolves. `analysis` is a registered analyzer name.
model: qwen2.5-7b-instruct
analysis: attention
